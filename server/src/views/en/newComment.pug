extends base

block content
  p Hello #{username},
  p A new comment is available on ProchainWeb.
  table.btn.btn-primary(role='presentation' border='0' cellpadding='0' cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation' border='0' cellpadding='0' cellspacing='0')
            tbody
              tr
                td
                  a(href=`${url}#comment` target='_blank') View comment
  p If you no longer wish to receive email notifications, you can unsubscribe by clicking the link below.
  p
    a(href=`${unsubscribeUrl}`) Unsubscribe
